@model List<ResultFeatureSliderDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <table class="table table-responsive">
                    <thead>
                        <tr>
                            <th class="sortable">#</th>
                            <th class="sortable">Image</th>
                            <th class="sortable">Title</th>
                            <th class="sortable">Description</th>
                            <th>Operations</th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        count++;
                        <tr>
                            <td class="text-truncate">@count</td>
                            <td class="text-truncate">
                                <img src="@item.ImageUrl" width="80px" />
                            </td>
                            <td class="text-truncate">@item.Title</td>
                            <td class="text-truncate">@item.Description</td>

                            <td class="text-truncate">

                                <button type="button" class="btn btn-outline btn-rounded btn-danger" data-toggle="modal" data-target="#modalDeleteSlider-@item.FeatureSliderId">
                                    <i class="fa fa-eraser"></i>
                                    
                                </button>

                                <a asp-route-id="@item.FeatureSliderId" asp-controller="FeatureSlider" asp-action="UpdateFeatureSlider" asp-area="Admin" data-id="@item.FeatureSliderId" class="btn btn-outline btn-rounded btn-sm btn-info"><i class="fa fa-edit"></i></a>
                            </td>
                        </tr>
                        <div class="modal" tabindex="-1" role="dialog" id="modalDeleteSlider-@item.FeatureSliderId">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title"><b>Attetion !!</b> Slider Will Be Deleted</h5>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        <p>@item.Title</p>
                                    </div>
                                    <div class="modal-footer">
                                        <a class="btn btn-danger" asp-route-id="@item.FeatureSliderId" asp-controller="FeatureSlider" asp-action="DeleteFeatureSlider" asp-area="Admin" data-id="@item.FeatureSliderId">Delete</a>
                                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                    </div>
                                </div>
                            </div>
                        </div>

                    }

                </table>
                <a class="btn btn-outline btn-rounded btn-sm btn-primary" asp-controller="FeatureSlider" asp-action="CreateFeatureSlider" asp-area="Admin">New Slider Entry</a>
            </div>
        </div>
    </div>
</section>